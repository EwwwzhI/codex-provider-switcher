# 验收报告

验收日期：2026-09-09，Windows 11 x64。所有写入测试均使用完全隔离的合成数据目录，未修改真实 `CODEX_HOME`。

## 已通过

- 源码自动化测试：56 项全部通过。
- 会话与项目：中文名称、长路径、无项目会话、空项目、归档会话和代理子会话。
- 切换：单选、多选、重复 ID 去重、重复切换，以及 `openai`、`wzh`、`seu` 间不同长度 Provider 名称的双向变更。
- 不变性：模型、全局配置、非目标会话文件和非目标数据库记录保持不变。
- 历史分页：会话元数据变长或变短后，起止偏移、结束偏移和历史投影位置一起调整，原历史事件字节保持不变。
- 故障：数据库锁定、会话文件缺失、ID/Provider 不一致、未知索引结构、索引越界、备份失败、预览后并发变化、文件替换失败和进程中断。
- 恢复：自动回滚、启动后未完成操作阻断、硬退出后恢复、恢复中断续行、备份校验失败和新增消息冲突拒绝覆盖。
- GUI：搜索、Provider 筛选、归档/代理显示、父子会话独立勾选、复制 ID、隐藏选中项计数、预览与工作线程应用。
- 显示：扩展盘符路径、扩展 UNC、普通 UNC、中文和长路径仅在 GUI 详情与 CLI JSON 中移除 `\\?\` 展示前缀；内部校验与写入继续使用原始路径。
- 清晰度：微软雅黑 UI 14px 正文、16px 章节标题和 18px 会话标题；主按钮使用真实粗体。100%、125%、150% 缩放截图分别为 1240×900、1550×1125、1860×1350，按钮及表单文字未裁切。
- 图标：1024×1024 PNG 主源图已生成，ICO 已验证包含 16、24、32、48、64、128、256 七个尺寸；GUI/CLI EXE 均嵌入新图标，GUI 运行时资源缺失时仍保留程序化降级图标。
- 独立产物：PyInstaller 单文件 CLI 可读取 JSON；GUI EXE 在未使用环境 Python 的进程中成功启动、读取合成数据并截图，中文字体正常。构建脚本会自动重复这两项产物级冒烟测试。

## 需要人工完成的真实验证

真实切换会修改用户数据，本次未在 Codex 运行期间代替用户执行。首次请按下列步骤验证：

1. 选中一个可丢弃的测试会话并查看变更清单。
2. 完全退出 Codex / ChatGPT、Codex CLI 及扩展后端，再应用切换。
3. 重新打开 Codex，确认原历史可读。
4. 手动发送一条简短请求，再在目标服务端记录中确认实际请求通道。

工具对本地字段与索引的验证通过，不代表目标 Provider 支持当前模型，也不代表实际网络请求已经通过该通道。
